{
  "name": "@ligat-fantasy/football-data",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": { "@ligat-fantasy/contracts": "workspace:*", "@ligat-fantasy/scoring": "workspace:*" },
  "devDependencies": {
    "@ligat-fantasy/typescript-config": "workspace:*",
    "@types/node": "^24.3.0",
    "vitest": "^3.2.4"
  }
}
